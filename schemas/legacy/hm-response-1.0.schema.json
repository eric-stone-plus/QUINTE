{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/eric-stone-plus/QUINTE/contracts/legacy-hm-response/1.0/schema.json",
  "title": "Historical QUINTE HM Response",
  "description": "Strict read compatibility for scheduler-owned responses created before the Primary Arbiter rename.",
  "type": "object",
  "additionalProperties": false,
  "required": ["hm_response_version", "run_id", "nonce", "policy_sha256", "evidence_packet_sha256", "input_receipt_sha256", "action_scope", "verdict"],
  "properties": {
    "hm_response_version": { "const": "1.0" },
    "run_id": { "type": "string", "minLength": 1 },
    "nonce": { "type": "string", "minLength": 32 },
    "policy_sha256": { "type": "string", "pattern": "^sha256:[a-f0-9]{64}$" },
    "evidence_packet_sha256": { "type": "string", "pattern": "^sha256:[a-f0-9]{64}$" },
    "input_receipt_sha256": { "type": "string", "pattern": "^sha256:[a-f0-9]{64}$" },
    "action_scope": { "type": ["string", "null"], "maxLength": 2000 },
    "verdict": { "$ref": "#/$defs/verdict" }
  },
  "$defs": {
    "verdict": {
      "type": "object",
      "additionalProperties": false,
      "required": ["arbiter_verdict_version", "summary", "recommendation", "residuals"],
      "properties": {
        "arbiter_verdict_version": { "const": "1.0" },
        "summary": { "type": "string", "minLength": 1, "maxLength": 20000 },
        "recommendation": { "type": "string", "minLength": 1, "maxLength": 20000 },
        "residuals": { "type": "array", "maxItems": 100, "items": { "$ref": "https://github.com/eric-stone-plus/QUINTE/contracts/lane-output/1.0/schema.json#/$defs/residual" } }
      }
    }
  }
}
